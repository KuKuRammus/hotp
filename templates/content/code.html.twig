{% extends 'layout/base.html.twig' %}

{% block body %}
    <h2>Code</h2>
    <div><b>Name:</b>{{ qrPayload.name }}</div>
    <div><b>Secret:</b>{{ qrPayload.secret }}</div>
    <div>
        <img src="{{ qrImage }}" alt="QR Image" />
    </div>
    <div>
        <a href="{{ path('check.index', { name: qrPayload.name }) }}">Go to check ></a>
    </div>
{% endblock %}
